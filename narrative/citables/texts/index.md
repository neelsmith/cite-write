# Citable passages of texts

## TL;DR

- The `CitableCorpus` package defines a `CitablePassage` type that implements the `CitableTrait`.
- A `CitablePassage` is identified by a `CtsUrn` and has text content.